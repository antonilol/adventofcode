const INPUT: &str = include_str!("../../input/{{NAME}}.txt");

fn main() {
    let answer_1 = INPUT.lines().fold(0, |acc, line| {
        acc
    });

    let answer_2 = INPUT.lines().fold(0, |acc, line| {
        acc
    });

    println!("answers: {answer_1} {answer_2}");
}
